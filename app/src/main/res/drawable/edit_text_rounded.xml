<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Focused State -->
    <item android:state_focused="true">
        <shape>
            <solid android:color="#FFFFFF" />
            <stroke android:width="2dp" android:color="#6200EE" /> <!-- Purple border -->
            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- Error State -->
    <item android:state_activated="true" android:state_selected="true" android:state_enabled="true">
        <shape>
            <solid android:color="#FFFFFF" />
            <stroke android:width="2dp" android:color="#B00020" /> <!-- Red border -->
            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- Default State -->
    <item>
        <shape>
            <solid android:color="#FFFFFF" />
            <stroke android:width="1dp" android:color="#CCCCCC" /> <!-- Grey border -->
            <corners android:radius="12dp" />
        </shape>
    </item>

</selector>
